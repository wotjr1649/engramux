{
  "source": "independent-agent-design-root-removed-prompt-answer-leak-before-solving",
  "cases": [
    {
      "id": "code-history",
      "prompt": "Continue from the earlier compatibility decision. Fix the tie-comparator maintenance bug in SelectZorrenEvents. Keep package probe and the existing exported type and function signature. Return up to limit events in descending Weight, using the previously agreed Sequence tie order, including at a full cutoff. Sequence is a unique arrival number, smaller means earlier. Preserve the exact cap, all struct fields and the input slice. Return an empty non-nil slice for non-positive limit or empty input. No imports or external state are necessary.",
      "history": "Earlier compatibility decision: at a full cutoff among equal-weight candidates, retain the candidate with the smaller arrival sequence and keep the exact requested cap; a later tie neither displaces the earlier candidate nor expands the result.",
      "starter": "package probe\n\ntype ZorrenEvent struct {\n ID string\n Kind string\n Weight int\n Sequence int\n Payload string\n}\n\nfunc SelectZorrenEvents(events []ZorrenEvent, limit int) []ZorrenEvent {\n if limit <= 0 { return []ZorrenEvent{} }\n selected := make([]ZorrenEvent, 0, limit)\n for _, event := range events {\n  position := 0\n  for position < len(selected) {\n   current := selected[position]\n   if current.Weight > event.Weight || (current.Weight == event.Weight && current.Sequence >= event.Sequence) {\n    position++\n    continue\n   }\n   break\n  }\n  if position >= limit { continue }\n  selected = append(selected, ZorrenEvent{})\n  copy(selected[position+1:], selected[position:])\n  selected[position] = event\n  if len(selected) > limit { selected = selected[:limit] }\n }\n return selected\n}\n"
    }
  ]
}
